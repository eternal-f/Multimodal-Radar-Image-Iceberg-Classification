KNN.py uses the result in previous file 'train_predict', and generate result in file 'knn' as 'features.csv'. 'filter_result.py' will delete some of the prediction in features.csv.  
KNN.py用之前CNN训练好的train_predict里的文件作为输入，会在knn文件夹下生成'features.csv'，'filter.py'用于对csv的结果进行删减，保留KNN的预测。
